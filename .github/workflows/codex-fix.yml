name: Codex Fix (Write Access)

on:
  issue_comment:
    types: [created]
  pull_request_review_comment:
    types: [created]

env:
  # JSON array of GitHub usernames allowed to trigger AI workflows in this repo.
  # Prefer setting a repository OR organization variable AI_WORKFLOW_AUTHORIZED_USERS_JSON
  # (Repo: Settings → Secrets and variables → Actions → Variables)
  # Example: ["mdc159"] or ["mdc159","teammate1"]
  AI_WORKFLOW_AUTHORIZED_USERS_JSON: ${{ vars.AI_WORKFLOW_AUTHORIZED_USERS_JSON || '[]' }}

jobs:
  codex-fix:
    # Only trigger on @codex-fix command from authorized users
    if: |
      (
        github.event_name == 'issue_comment' ||
        github.event_name == 'pull_request_review_comment'
      ) &&
      contains(github.event.comment.body, '@codex-fix') &&
      contains(fromJSON((startsWith(vars.AI_WORKFLOW_AUTHORIZED_USERS_JSON, '[') && vars.AI_WORKFLOW_AUTHORIZED_USERS_JSON) || '[]'), github.event.comment.user.login)

    runs-on: ubuntu-latest

    permissions:
      contents: write # Allow creating branches and editing files
      pull-requests: write # Allow creating and updating pull requests
      issues: write # Allow commenting on and updating issues
      id-token: write # Required for OIDC authentication
      actions: read # Read CI results

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 # Full history for better context

      - name: Configure Git
        run: |
          git config user.name "codex-bot"
          git config user.email "codex-bot@users.noreply.github.com"

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Get Issue/PR Number
        id: get-number
        run: |
          if [ "${{ github.event_name }}" == "issue_comment" ]; then
            echo "number=${{ github.event.issue.number }}" >> $GITHUB_OUTPUT
          else
            echo "number=${{ github.event.pull_request.number }}" >> $GITHUB_OUTPUT
          fi

      - name: Fetch issue/PR details
        id: fetch-details
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # Fetch the issue or PR details using GitHub CLI
          if [ "${{ github.event_name }}" == "issue_comment" ]; then
            gh issue view ${{ steps.get-number.outputs.number }} --json title,body,comments --jq '{title,body,comments: [.comments[] | {author: .author.login, body: .body}]}' > /tmp/issue-details.json
          else
            gh pr view ${{ steps.get-number.outputs.number }} --json title,body,comments --jq '{title,body,comments: [.comments[] | {author: .author.login, body: .body}]}' > /tmp/issue-details.json
          fi

      - name: Prepare fix instructions
        run: |
          # Read the template and replace placeholders
          # Support both layouts:
          # - This repo: prompt templates at repo root
          # - Copied into other repos: templates under .github/
          TEMPLATE_PATH=".github/issue_fix_prompt.md"
          if [ ! -f "$TEMPLATE_PATH" ]; then
            TEMPLATE_PATH="issue_fix_prompt.md"
          fi
          if [ ! -f "$TEMPLATE_PATH" ]; then
            echo "❌ Could not find issue fix prompt template at .github/issue_fix_prompt.md or issue_fix_prompt.md"
            exit 1
          fi

          INSTRUCTIONS=$(cat "$TEMPLATE_PATH" | sed 's/{AI_ASSISTANT}/codex/g')

          # Read issue/PR details
          ISSUE_DETAILS=$(cat /tmp/issue-details.json)

          # Prepend context with full issue details
          echo "# GitHub Context" > /tmp/codex-fix-prompt.md
          echo "- Repository: ${{ github.repository }}" >> /tmp/codex-fix-prompt.md
          echo "- Issue/PR Number: ${{ steps.get-number.outputs.number }}" >> /tmp/codex-fix-prompt.md
          echo "- Triggered by: ${{ github.event.comment.user.login }}" >> /tmp/codex-fix-prompt.md
          echo "" >> /tmp/codex-fix-prompt.md
          echo "## Issue/PR Details" >> /tmp/codex-fix-prompt.md
          echo '```json' >> /tmp/codex-fix-prompt.md
          echo "$ISSUE_DETAILS" >> /tmp/codex-fix-prompt.md
          echo '```' >> /tmp/codex-fix-prompt.md
          echo "" >> /tmp/codex-fix-prompt.md
          echo "$INSTRUCTIONS" >> /tmp/codex-fix-prompt.md

      - name: Run Codex Fix
        id: codex
        uses: openai/codex-action@v1
        timeout-minutes: 30
        with:
          openai-api-key: ${{ secrets.OPENAI_API_KEY }}

          # Fix-specific instructions loaded from template
          prompt-file: /tmp/codex-fix-prompt.md

          # Output file for Codex results
          output-file: codex-output.md

          # Configure Codex for workspace write access
          sandbox: workspace-write

      - name: Prepare PR body
        if: success()
        run: |
          # Create PR body with Codex output
          echo "## Automated Fix by Codex" > /tmp/pr-body.md
          echo "" >> /tmp/pr-body.md
          echo "This PR was automatically generated by Codex in response to issue #${{ env.number }}." >> /tmp/pr-body.md
          echo "" >> /tmp/pr-body.md

          # Include Codex output if it exists
          if [ -f codex-output.md ]; then
            echo "## Codex Analysis" >> /tmp/pr-body.md
            echo "" >> /tmp/pr-body.md
            cat codex-output.md >> /tmp/pr-body.md
            echo "" >> /tmp/pr-body.md
          fi

          echo "---" >> /tmp/pr-body.md
          echo "*Fixed by Codex*" >> /tmp/pr-body.md

      - name: Create pull request with fixes
        if: success()
        id: create-pr
        uses: peter-evans/create-pull-request@v6
        with:
          commit-message: "fix: resolve issue via Codex"
          branch: fix/issue-${{ steps.get-number.outputs.number }}-codex
          title: "Fix: Automated fix via Codex"
          body-path: /tmp/pr-body.md

      - name: Comment on issue
        if: steps.create-pr.outputs.pull-request-number
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const issueNumber = ${{ steps.get-number.outputs.number }};
            const prNumber = ${{ steps.create-pr.outputs.pull-request-number }};
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: issueNumber,
              body: `✅ Codex created PR #${prNumber} to fix this issue`
            });

  unauthorized-message:
    # Post message for unauthorized users
    if: |
      (
        github.event_name == 'issue_comment' ||
        github.event_name == 'pull_request_review_comment'
      ) &&
      contains(github.event.comment.body, '@codex-fix') &&
      !contains(fromJSON((startsWith(vars.AI_WORKFLOW_AUTHORIZED_USERS_JSON, '[') && vars.AI_WORKFLOW_AUTHORIZED_USERS_JSON) || '[]'), github.event.comment.user.login)

    runs-on: ubuntu-latest

    permissions:
      issues: write
      pull-requests: write

    steps:
      - name: Post unauthorized message
        uses: actions/github-script@v7
        with:
          script: |
            const comment = {
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: `❌ @${context.actor} - You are not authorized to trigger Codex fixes.\n\nOnly maintainers can trigger Codex: Please ask a maintainer to run the fix command.`
            };

            if (context.eventName === 'issue_comment') {
              await github.rest.issues.createComment({
                ...comment,
                issue_number: context.issue.number
              });
            } else if (context.eventName === 'pull_request_review_comment') {
              await github.rest.pulls.createReplyForReviewComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                pull_number: context.payload.pull_request.number,
                comment_id: context.payload.comment.id,
                body: comment.body
              });
            }
